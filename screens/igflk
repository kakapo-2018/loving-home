<TouchableOpacity 
            onPress={() => this.showMission()}>

          <View style={styles.hidden}>
          
          <Text style={!this.state.hidden ? { height: 0 } : styles.hidden}>hi</Text>

          </View>
          </TouchableOpacity>


<CheckBox 
            title="support"
            checked={false} />

          style={this.state.hidden ? styles.hidden : {height:10}}



           <TouchableOpacity 
            onPress={() => this.showMission()}>
            <View style={styles.hidden}>
            <View style={this.state.hidden ? styles.hidden: { height: 0 }}>
            <Image 
            source={{ uri: item.logo }} />
            </View>
            </View>
            </TouchableOpacity>





             {/* <TouchableOpacity 
            onPress={() => this.showMission()}>

          <View style={styles.hidden}>
          
          <Text style={!this.state.hidden ? { height: 0 } : styles.hidden}>hi</Text>

          </View>
          </TouchableOpacity> */}
          {/* <Overlay isVisible={this.state.hidden}>
            <Text>Hello friend</Text>
          </Overlay> */}
          {/* <View style={[styles.overlay, { height: 360}]} /> */}
        {/* <FlatList
          horizontal={true}
          data={this.props.charities.Charities}
          keyExtractor={this.keyExtractor}
          renderItem={({ item }) => (

            <View style={styles.infoholder}>

          <TouchableOpacity 
            onPress={() => this.showMission()}>

  
          <View style={{backgroundColor: 'rgba(0,0,0,0.5)'}}>
            <ImageBackground style={{height: 200, width: 200, zIndex: -1}} source={{ uri: item.logo }}>
              <Text style={styles.pageheader}>{item.charityName}</Text>
              <Text style={styles.mission}>{item.mission}</Text>
              <CheckBox 
              title="support"
              checked={false} />
            </ImageBackground>
          </View>
          </TouchableOpacity> */}




         
   

            {/* <Text style={!this.state.hidden ? { height: 0 } : styles.hidden}>
              {item.mission} 
            </Text> */}

            {/* </View> */}
           
            
            

           
            
            
          {/* )}

        /> */}